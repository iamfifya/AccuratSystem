<Window x:Class="AccuratPanelCWD.AddEditEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AccuratPanelCWD"
        xmlns:controls="clr-namespace:AccuratPanelCWD.Controls"
        Title="Редактирование сотрудника"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize" 
        Width="700" Height="850" 
        Background="{DynamicResource BgMain}"
        BorderBrush="{DynamicResource BorderColor}"
        BorderThickness="1"
        WindowStyle="SingleBorderWindow"
        Foreground="{DynamicResource TextMain}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Область со скроллом -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,20">

                <!-- Заголовок -->
                <TextBlock Text="{Binding Title}" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,25" 
                           HorizontalAlignment="Center"/>

                <!-- ФИО -->
                <TextBlock Text="ФИО" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <TextBox Text="{Binding CurrentEmployee.FullName, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,20" 
                         Height="45" 
                         Padding="15,0"
                         FontSize="14"/>

                <!-- Телефон -->
                <TextBlock Text="📱 Телефон" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="PhoneTextBox" 
                         Text="{Binding CurrentEmployee.Phone, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,5" 
                         Height="45" 
                         Padding="15,0" 
                         FontSize="14"
                         PreviewTextInput="PhoneTextBox_PreviewTextInput"
                         TextChanged="PhoneTextBox_TextChanged" 
                         MaxLength="20"/>
                <TextBlock Text="Формат: 89996094363" 
                           Foreground="{DynamicResource TextMuted}" 
                           Margin="0,0,0,20" 
                           FontSize="11" 
                           HorizontalAlignment="Left"/>

                <!-- Логин -->
                <TextBlock Text="Логин" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <TextBox Text="{Binding CurrentEmployee.Login, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,20" 
                         Height="45" 
                         Padding="15,0"
                         FontSize="14"/>

                <!-- Пароль -->
                <TextBlock Text="Пароль" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox" 
                             Margin="0,0,0,20" 
                             Height="45" 
                             Padding="15,0" 
                             FontSize="14"
                             VerticalContentAlignment="Center"/>

                <!-- Должность -->
                <TextBlock Text="Должность" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <controls:CustomComboBox x:Name="RoleComboBox" 
                                         Margin="0,0,0,20" 
                                         Height="45" 
                                         Placeholder="Выберите должность"
                                         DisplayMemberPath="Name" 
                                         SelectedValuePath="Id"
                                         SelectedValue="{Binding CurrentEmployee.RoleId, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Секция зарплаты -->
                <Border Background="{DynamicResource BgCard}" 
                        CornerRadius="6" 
                        Padding="20" 
                        Margin="0,5,0,20"
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="💰 Параметры оплаты"
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="{DynamicResource TextMain}" 
                                   Margin="0,0,0,15"/>

                        <!-- Ставка зарплаты -->
                        <TextBlock Text="Ставка зарплаты (%)" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMain}"
                                   Margin="0,0,0,8"/>
                        <TextBox Text="{Binding CurrentEmployee.BaseWagePercentage, UpdateSourceTrigger=PropertyChanged}" 
                                 Margin="0,0,0,15" 
                                 Height="45" 
                                 Padding="15,0"
                                 FontSize="14"/>

                        <!-- Фиксированный оклад -->
                        <TextBlock Text="Фиксированный оклад за смену (₽)" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMain}"
                                   Margin="0,0,0,8"/>
                        <TextBox x:Name="SalaryTextBox" 
                                 Height="45" 
                                 Padding="15,0" 
                                 VerticalContentAlignment="Center"
                                 FontSize="14"
                                 Text="{Binding CurrentEmployee.BaseSalaryPerShift, UpdateSourceTrigger=PropertyChanged, FallbackValue=0}"/>
                    </StackPanel>
                </Border>

                <!-- Филиал привязки -->
                <TextBlock Text="📂 Филиал привязки" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextMain}"
                           Margin="0,0,0,8"/>
                <controls:CustomComboBox x:Name="BranchComboBox" 
                                         Margin="0,0,0,20" 
                                         Height="45" 
                                         Placeholder="Выберите филиал"
                                         DisplayMemberPath="Name" 
                                         SelectedValuePath="Id"
                                         SelectedValue="{Binding CurrentEmployee.BranchId, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Чекбокс трудоустройства -->
                <CheckBox Content="Трудоустроен" 
                          IsChecked="{Binding CurrentEmployee.IsActive}" 
                          Margin="0,10,0,0" 
                          HorizontalAlignment="Left" 
                          Foreground="{DynamicResource TextMain}"
                          FontSize="14"
                          FontWeight="SemiBold"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20">
            <Button Content="✓ Сохранить" 
                    Click="SaveButton_Click" 
                    Style="{StaticResource SuccessBtn}"/>
            <Button Content="✗ Отмена" 
                    Click="CloseButton_Click" 
                    Style="{StaticResource DangerBtn}"/>
        </StackPanel>
    </Grid>
</Window>